import React from 'react';

const Home = (props) => (
	<div>
		<h1>Hello World!</h1>
		<h3>
			My name is Alec Armstrong and this is an app demostrating the use
			of React-Router.
			<br/>
			Click the links on the navbar to see routing in action.
		</h3>
	</div>
);

export default Home;
